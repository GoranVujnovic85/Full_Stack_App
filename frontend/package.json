{
  "name": "frontend",
  "version": "1.0.0",
  "description": "npx vue-tsc --noEmit  --> Error checking in the terminal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "vue-router": "^4.5.0"
  }
}
